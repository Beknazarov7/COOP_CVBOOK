# Nixpacks configuration for CV Book Django project
[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build phase - skipping collectstatic'"]

[start]
cmd = "bash start.sh"

